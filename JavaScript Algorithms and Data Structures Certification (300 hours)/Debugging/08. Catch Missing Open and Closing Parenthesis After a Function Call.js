// Fix the code so the variable result is set to the value returned from 
// calling the function getNine.

// (1) Your code should fix the variable result so it is set to the number 
// that the function getNine returns.
// (2) Your code should call the getNine function.


function getNine() {
    let x = 6;
    let y = 3;
    return x + y;
}

let result = getNine();
console.log(result);
